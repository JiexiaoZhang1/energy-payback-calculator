# Screenshot Index / 截图索引

This page lists the screenshots used in the README and documentation.

这里列出 README 和文档中使用的截图。

## 1. Chinese Input Screen / 中文输入页

File:

```text
docs/assets/screenshot-input-zh.png
```

![Chinese input screen](assets/screenshot-input-zh.png)

Shows:

- Chinese language mode
- CNY display
- 24-hour tariff timeline
- Negative, low, and high price segments
- Four flexible device cards
- Total equipment cost input
- Advanced settings entry point

展示内容：

- 中文界面
- CNY 货币显示
- 24 小时电价时间轴
- 负价、低价、高价时段
- 四类可调度设备卡片
- 设备总成本输入
- 高级参数入口

## 2. Tariff Editor Drawer / 电价编辑抽屉

File:

```text
docs/assets/screenshot-tariff-editor-zh.png
```

![Tariff editor drawer](assets/screenshot-tariff-editor-zh.png)

Shows:

- Bottom-sheet editing interaction
- Start and end time steppers
- Negative price input
- Save, cancel, and delete actions
- The selected tariff block highlighted behind the drawer

展示内容：

- 底部抽屉式编辑交互
- 开始和结束时间步进器
- 负电价输入
- 保存、取消、删除操作
- 背景中被选中的电价时段

## 3. English Result Screen / 英文结果页

File:

```text
docs/assets/screenshot-result-en.png
```

![English result screen](assets/screenshot-result-en.png)

Shows:

- English language mode
- Backend-calculated status
- Estimated payback time
- Estimated annual savings
- Savings by device category
- Daily energy plan
- Share and edit actions

展示内容：

- 英文界面
- 后端计算状态
- 预计回本时间
- 预计年节省
- 各设备节省贡献
- 每日用电计划
- 分享和修改按钮

## 4. Desktop Framing / 桌面宽屏展示

File:

```text
docs/assets/screenshot-desktop-en.png
```

![Desktop screenshot](assets/screenshot-desktop-en.png)

Shows:

- How the mobile-first app is framed on a wider desktop viewport
- Centered app shell
- Same input workflow in English

展示内容：

- 移动优先应用在桌面宽屏上的呈现方式
- 居中的应用外壳
- 英文输入流程

## Screenshot Capture Notes / 截图生成说明

The screenshots were captured from the locally built production app served by the Express server at:

```text
http://localhost:8787
```

They are real rendered screenshots, not mock images.

这些截图来自本地构建后的生产页面，由 Express 服务托管，地址是 `http://localhost:8787`。它们是真实渲染截图，不是手工拼的 mock 图。
